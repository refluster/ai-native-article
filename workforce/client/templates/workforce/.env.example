# Bearer token issued by the workforce operator. Required for the
# `POST /agents/{slug}/engagements` audit POST-back. Single shared token
# per Phase 7 PR5 scope — the operator decides distribution.
#
# Copy this file to `.env` (which IS gitignored) and paste the token.
# Then add `.workforce/.env` to your repo's `.gitignore`.
WF_TOKEN=
