# Memory → Lint Promotion Backlog

**Layer:** L2 registry (this file is data; `scripts/check-governance-registries.mjs` keeps its table well-formed — R-12).
**Decision record:** [ADR-0001](adr/adr-0001-self-driving-governance-mechanisms.md) (the memory→lint ratchet, Engine A).
**Audience:** the operator, and any agent running a governance retrospective (governance.md §6).

---

## 0. What this is

A rule that gets **violated twice is no longer a memory item — it is a missing
mechanical check.** This backlog is the audit trail for that promotion: it records
every recurring failure mode, how many times it has bitten us, and whether it has
been promoted to an enforced regulation (an `R-NN` row in governance.md §4), accepted
as a tolerated risk (a row in [risk-acceptance-ledger.md](risk-acceptance-ledger.md)),
or is still being watched.

Without this file, the same class of bug returns and the fix is re-discovered from
scratch each time. With it, "why does this lint exist?" always has an answer.

## 1. Promotion rule

> A backlog entry is **promoted to an `R-NN` regulation** when it has been
> **violated ≥ 2 times within 90 days**, OR when it has sat in `watching` for
> ≥ 6 months without revision (stability — the rule is real, just rarely tripped).

When an incident reveals a new failure mode:

1. **Append a row** to §3 with `Status: watching` and `Count: 1` (cite the incident ref — a commit SHA, issue, or date).
2. On the **second** occurrence inside 90 days, bump `Count`, set `Status: accepted`, and open a PR that adds the lint/guard. Set `Promoted via` to the `R-NN` id once it lands.
3. If the failure mode is **not worth a machine check** (too rare, too costly to detect, intrinsically a judgment call), set `Status: declined` and add a one-line reason — or move it to the risk-acceptance ledger instead.

This is the §6 "governance retrospective" loop made into a durable ledger rather than a one-time doc edit.

## 2. Status vocabulary

| Status | Meaning |
|---|---|
| `watching` | Seen once. Recorded so a second occurrence is recognised, not re-discovered. |
| `accepted` | Crossed the threshold (≥2 in 90d, or ≥6mo stable). A lint/guard is owed; `Promoted via` names it once it lands. |
| `promoted` | The lint/guard is live in CI/hooks. `Promoted via` points at the `R-NN` id. |
| `declined` | Deliberately not mechanised. The reason lives in the row; consider the risk-acceptance ledger instead. |

## 3. Backlog

<!-- registry:memory-lint columns: ID | Rule | Incidents | Count | Status | Promoted via -->

| ID | Rule | Incidents | Count | Status | Promoted via |
|---|---|---|---|---|---|
| ML-001 | A truncated article body must never reach gh-pages; the generation path must fail loud on a cut-off / empty / LLM-failure body. (2026-06-28: the original `finish_reason==='length'` framing was GAS-specific; the GAS engine is retired and the heuristic hand-sync this row tracked — `article-health.mjs` ↔ `Code.gs#isTruncatedMarkdown` — no longer exists: every consumer now imports the single `scripts/lib/truncation.mjs`. Rule still enforced, via W-1 + R-10.) | d17e1d58ec42 (2026-05-03) | 1 | promoted | W-1 (`article-level{2,3}/publish-notion.mjs`, generation-time) + R-10 (pre-deploy gate). Historical: R-3 / R-4 / R-5 (retired GAS runtime throws). |
| ML-002 | An edit to an L1 framework law must announce itself (cite the doc or opt out), so undocumented rule drift can't ship. | azure-budget bracket was undocumented when the L2 truncation bug landed (2026-05-03) | 1 | promoted | R-11 (L1 citation gate) |
| ML-003 | Prompts bundled inside generation-engine code hide content-generation diffs inside unrelated function changes; a prompt-version bump should ship as its own versioned PR (AGENTS.md §2 rule 11). (2026-06-28: originally about inline prompts in the retired `Code.gs`; the concern now applies to the workforce `article-level{2,3}` cadences, where the rule-11 per-PR prompt-bump discipline addresses it.) | design-policy.md §2 declared the commitment (2026-05-16) | 1 | watching | — |
| ML-004 | A `routeKey` added to `agents-api/handler.ts` must ship with a matching `HttpApi` event in `workforce/infra/sam/template.yaml`; otherwise the route 404s in prod while every check stays green (`check-api-routes.mjs` compares template↔live, not handler↔template). | PR #274 added `GET /stats` to the handler only; console-wide outage 2026-06-10 | 1 | watching | — |
| ML-005 | A non-test caller that passes a literal `wf/...` name to `getSecret`/`getSecretRaw` is a credential-resolution bypass: it skips the Epic-010 typed→`_default`→legacy tiering (`project.ts getCredential` / `llm-anthropic resolveAnthropicApiKey`) and so fails only at runtime — silently, on an async path — once the migration deletes the bare key. Candidate gate when it recurs: grep `getSecret(Raw)?\(["']wf/` outside `shared/{secrets,project,llm-anthropic}.ts` + `*-tests.ts`. | `llm-anthropic.ts` hardcoded `getSecret("wf/anthropic")` while Epic-010 §6 moved the key to `wf/projects/_default/anthropic.api_key`; the bare secret was never (re)stored, so every Lambda-side `complete()` (messaging-reply, memory-compactor) threw at key-read — the second `/messaging` no-reply, root-caused from prod logs via the #284 diagnostic and fixed by tiered resolution (PR #281) + operator storing the typed secret 2026-06-10 | 1 | watching | — |
| ML-006 | The W-1/R-10 truncation heuristic (the shared `scripts/lib/truncation.mjs`) must treat a trailing emphasis-close (`*…。*` italic byline) as a complete ending; a terminal-glyph set that omits `*`/`_` turns every persona-byline article into a false C-1 violation and blocks all deploys. The generation path (workforce article-level2/3) carries the same cut-off guard at publish time (W-1), so truncation is caught at generation, not only at R-10 where one bad body blocks the whole site deploy. (2026-06-28: with the GAS engine removed, the workforce `publish-notion.mjs` W-1 guard is the sole generation-time enforcer; both import the single canonical `truncation.mjs`.) | e7fc028993e1 byline `*Elena は…ご確認ください。*` flagged as truncated; deploy runs #469/#470/#485 red 2026-06-10/11 (fix: unwrap trailing `[*_]+` before the glyph test in the shared `truncation.mjs` — then imported by the now-retired GAS `Code.gs` and the article-level2/3 `publish-notion.mjs` W-1 guard alike) | 1 | watching | — |
| ML-007 | A roster model whose `complete()` request shape isn't pinned by a per-model wire-shape test fails only at runtime, and only for the personas on that model — Anthropic removes/renames params across model generations (`thinking:{enabled,budget_tokens}` and `temperature` removed on Opus 4.7+), so a shape that works on sonnet silently 400s on opus. | maya (roster's only `opus-4-7`) never replied on /messaging while sonnet personas worked, 2026-06-11; root cause: legacy thinking shape + temperature sent to a model where both are removed. Fix: model-capability gates in `llm-anthropic.complete()` + per-model wire-shape unit tests | 1 | watching | — |
| ML-008 | A list/index endpoint that returns a deliberately-truncated field (`body_preview`, an excerpt) which the renderer treats as the complete value ships a mid-sentence cut with no expand affordance — a C-1/W-1-class defect that passes every test because the field is *valid*, just short. Candidate guard when it recurs: a view-builder convention (or lint) that a list response either carries the full field, or an explicit `truncated`/`has_more` flag the renderer must honour — never a bare preview the client renders as whole. | `GET /feed` + `/agents/{slug}/posts` returned only `body_preview` (≤320c); the SPA rendered it as the body, so every >320c post was cut and PostCard's read-more never fired (it only received the preview). Fixed PR #310 by hydrating full `body` server-side (`resolveFeedBody`/`toFeedPostListView`) + a bounded, logged S3-miss fallback (W-4). | 1 | watching | — |
| ML-009 | Every escalation of a PR to a human MUST stamp `autopilot:needs-human`, regardless of *who* escalates. The pr-autopilot skill enforces this mechanically on its write-path (`workforce/skills/pr-autopilot/pr-autopilot-post.mjs` adds `ESCALATION_LABEL` from the `--needs-human` flag OR the hidden `<!-- autopilot:needs-human -->` body marker), but a **session-driven** escalation — a Claude Code / persona-in-session reviewer handing a PR to the operator without going through that script — bypasses the label entirely, so the operator's `is:open label:autopilot:needs-human` queue silently misses it. Guard (this PR): `workforce/scripts/check-escalation-labels.mjs` — flags any open PR whose comments carry the `<!-- autopilot:needs-human -->` hand-off marker but lack the label, catching session-driven misses regardless of code path. CI wiring (a workflow / the daily `wf-audit` Lambda) is the Zone-A follow-up. | (1) #358 (project-ind) touched the L0/L1 path `workforce/docs/data-model.md`; pr-autopilot correctly escalated to the operator in-session (R-N10 / adr-0010) but the PR was not labelled `autopilot:needs-human` until the operator flagged the miss (2026-06-22); label applied retroactively before the operator-approved merge. (2) #362 (pr-merge self-approve fix): the session reviewer handed the merge decision to the operator **in chat** without the label, until the operator flagged it again (2026-06-22); label applied via `pr-autopilot-post.mjs --needs-human`. Two session-driven misses in one day = the recurrence the ratchet promotes on. | 2 | promoted | R-13 — daily terminal-state sweep (`workforce-pr-terminal-sweep.yml` → `pr-autopilot-sweep.mjs --apply`, which auto-labels the marker-but-no-label class); `workforce:escalation-labels` remains the narrow state lint |
| ML-010 | A public asset overwritten **in place** at a stable CloudFront-fronted key (the podcast `feed.xml`) must carry a short `Cache-Control` (or trigger a CloudFront invalidation on write), else the edge keeps serving the stale object after S3 is updated — the change is invisible until the distribution's default TTL expires, and every test stays green because the S3 object is correct. Fresh, immutable keys (per-episode MP3s) are exempt — a new key is always a cache miss. Candidate guard when it recurs: assert any in-place-overwritten public S3 key in `wf-podcast` (and similar publishers) sets `CacheControl`, or the publish flow invalidates that path. | `wf-podcast` `buildRss` `PutObject` set `ContentType` but no `Cache-Control`; after `podcast-publish` wrote a 2-episode `feed.xml` to S3, CloudFront kept serving the cached 1-episode feed (2026-06-29). Fixed by `CacheControl: max-age=300` on the feed `PutObject` + a one-off manual `/podcast/feed.xml` invalidation. | 1 | watching | — |
| ML-011 | A `pr-autopilot` run must drive the FULL cycle — seat a panel of **≥3 reviewers**, post every review + the verdict as PR comments, then merge (predicate holds) or escalate with the `autopilot:needs-human` label — never stop at a lone routing comment, a 1–2 reviewer pass, or a draft→mergeable flip with no review posted. The soft "1-3 reviewers" wording let an under-reviewed or review-less run read as "done". Guard (this PR): `pr-merge.mjs` `MIN_REVIEWERS=3` fails closed on any sub-3 green panel server-side (re-verified, not trusted from the decision), the SKILL.md operator-mandate box makes the four terminal requirements explicit, and the binding `nomination_rules` seat ≥3 by construction. | Operator report 2026-06-29: recent runs flipped a draft toward mergeable without posting review comments, and/or assigned a single reviewer — the #530/#514 "stuck at a lone routing comment" failure recurring as under-review. | 1 | accepted | `MIN_REVIEWERS=3` floor in `workforce/skills/pr-autopilot/pr-merge.mjs` + reinforced SKILL.md mandate (skill 0.15.0). Since 2026-07-03 the R-13 terminal-state sweep additionally converts a stalled (review-less) run into an escalation instead of a silent non-state. Candidate `R-NN` if a sub-3 / review-less run recurs despite the floor. |
| ML-012 | A persona/agent reference in any externally-posted body (routing comments, reviews, verdicts) must never be a raw GitHub `@`-mention — persona slugs are not GitHub accounts, and GitHub notifies whichever real user owns the name. Canonical agent-reference format: `wf:<slug>` in backticks; literal `@…` tokens (scoped npm packages, decorators) are quoted in backticks, where GitHub never linkifies. | pr-autopilot routing comments @-mentioned nominated personas per the SKILL.md template; `@yuki` notified the unrelated real user github.com/yuki (operator report 2026-07-04). | 1 | accepted | Raw-@-mention refusal in `workforce/skills/pr-autopilot/pr-autopilot-post.mjs` (`findRawMentions`, exit 1) + the SKILL.md `wf:<slug>` convention (skill 0.17.0). Candidate `R-NN` (repo-wide posted-body lint) if a raw mention recurs via another write path. |
| ML-013 | A judgment-layer cadence can converge on a degenerate all-skip steady state — every fire "succeeds" (a clean EXEC row, a reasoned skip note) while producing zero deliverables — and no signal turns red, because each individual skip is locally defensible; the failure is only visible to a human reading consecutive ledger rows. A prompt-level fix (e.g. deliver-by-default) has no mechanical detector for its own recurrence: a persona can walk the new decision ladder and still talk itself out of every rung. Candidate gate when it recurs: a threshold sweep over execution rows — N consecutive zero-deliverable fires on one binding (start: N=5 for daily cadences) ⇒ flag an issue / feed alert, e.g. as a leg of the daily R-13 sweep or the `wf-audit` Lambda. | `daily-research` v0.3.0 skip-by-default: cohort-2 broad beats converged on ~100% skip — Maya's founder-strategy binding skipped 11/11 displayed fires through 2026-07-05 — caught only by the operator reading the RUNS·DELIVERABLES ledger; fixed at the prompt layer by v0.4.0's deliver-by-default output ladder (PR #440; panel finding D2 named the missing detector). | 1 | watching | — |
| ML-014 | A persona's title lives as multiple hand-written copies with no shared source and no consistency check: the `AGENT#{slug}/META.role` field, the `system_prompt` header (`# Name — Title — Location`) and body, every colleague prompt's inline cross-reference ("You report to X (City, Title)"), and doc/roster tables. A title change applied to one copy silently strands the others — and the stalest copy (the W-5-protected prompt) is the one every generated artefact inherits its self-description from. Guard: `role` ↔ prompt-header-title equality is enforced at the agents-api write boundary on the EFFECTIVE row whenever either field is written (`S19-role-prompt-title` in `workforce/lambdas/shared/agent-config.ts`, wired into PATCH and POST /agents); the stock/cross-reference auditor is `workforce/scripts/check-identity-drift.mjs` (cross-refs are denormalized by construction — advisories, fixed opportunistically on each prompt bump). | AGENT#maya: `role` flipped Founder→President (mid-June 2026, pre-audit-trail — no AUDIT item records it; the trail starts 2026-06-12) while her prompt kept "Founder" (last bumped 2026-05-27 for the Q2 PM-delegation `role: "Founder"` clean drop). New seeds from #374 (2026-06-26) onward said "President" while `silas-system.md` (2026-06-14) still said "Founder" — the eras are readable in the copies. Surfaced when the 2026-07 monthly report signed Maya as Founder from her prompt and the operator caught it (2026-07-06). Sweep at introduction: 1/33 header↔role drift (maya), 38 stale cross-references. | 1 | accepted | `S19-role-prompt-title` write-boundary guard + `check-identity-drift.mjs` stock auditor (this PR). Candidate `R-NN` if a drifted copy recurs through a path the write boundary doesn't see. |
| ML-015 | A per-project read route that resolves a project-scoped credential must treat "record configures the integration but the secret is not provisioned" as an empty/unprovisioned result, not an unhandled throw — and an index page that fans out over N projects must isolate per-project failures (visible warning + render the rest), or one unprovisioned project blanks the whole surface. Candidate gate when it recurs: a handler-layer convention/test that every `wf/projects/{id}/<type>` GetSecretValue caller has an explicit ResourceNotFoundException branch. | `GET /projects/conference/reports` 500'd (`ResourceNotFoundException`: conference has `github.token` in `credential_types` but the secret was never provisioned); the console `/reports` index `Promise.all` then blanked entirely with "Failed to load the report index" (operator report 2026-07-21). Fixed: reports.ts maps the missing secret to "no reports provisioned" (mirrors credentials-LIST), SPA switches to `allSettled` + per-project failure banner. | 1 | watching | — |
| ML-016 | A follow-up/backlog row must not enter the terminal `✅ done` state while its own stated verification step is unrun. "Shipped" and "verified" are different facts, and a status vocabulary offering only `done` forces authors to encode the difference in trailing prose (`✅ done (auto-deployed) — verification step outstanding`) — which the downstream LLM summarisers (`workforce/skills/monthly-report/SKILL.md:41`, `vp-monthly-report/SKILL.md:62`) will **plausibly** drop, so the row reports complete. Precision note (PR #526 cycle-1, lens dario D7): neither skill contains a token-bucketing step — both read `follow-ups.md` as prose ("open items in …"). The harm is an inference about how an LLM reader treats a `✅`-marked table, not an observed mis-bucketing; no run has been recorded where it happened. The guard below does not depend on this mechanism — a row asserting done while its own check is unrun is wrong regardless of who reads it. A second shape of the same defect: closing a row as `⛔ obsoleted` because its *target* was deleted, without re-filing the part of its *intent* that survived the deletion — the closure ships and its residue does not. Guard (this PR): a `🟡 shipped — verification outstanding (<the unrun check>)` state added to the `workforce/docs/follow-ups.md` schema, plus an explicit schema note that `⛔ obsoleted` requires a successor row when the intent survives. Candidate `R-NN` when it recurs: a lint over `follow-ups.md` rejecting a `✅` row whose Notes contain an outstanding-verification phrase. | (1) OP-008 `✅ done 2026-06-12` closes with "Remaining eyeball check: `/agents` shows L0/L1/L2 tiers and `/org?center=elena` renders the graph" — unrun. (2) PR #523 (2026-07-31) flipped OP-011 to `✅ done (auto-deployed) — verification step outstanding` with the `GET /performance` join check unrun, and FU-021 to `✅` with its ≥1-week soak unrun (and, per FU-032, unwitnessable); the same PR closed FU-013a/b as `⛔ obsoleted` with an issue diff of 0 filed. Two occurrences inside 90 days = the recurrence the ratchet promotes on (lens farah F5, PR #523 cycle-1). | 2 | accepted | **Guard shipped (PR #526 cycle-1):** `checkFollowUpsStatuses()` in `scripts/check-governance-registries.mjs` fails CI on any `## Open` row whose Status is terminal `✅` while its Status or Notes name an outstanding verification — already wired into the R-12 `check-registries` CI step, so no workflow change was needed. It caught OP-008 (occurrence 1) on its first run; that row is now `🟡`. Supported by the `🟡 shipped` state + the `⛔ obsoleted` successor-row requirement in the `follow-ups.md` schema block. **Owed next:** the statute row (`R-NN` in governance.md §4) naming this gate — an L1 edit, so it is proposed for operator sign-off rather than self-merged; the mechanical check does not wait on it. |
| ML-017 | Node's built-in `fetch()` does not read `HTTPS_PROXY`, so any workforce script that calls it bypasses the CCR agent proxy entirely and is rejected upstream as `Host not in allowlist: <host>` — a message that blames the egress policy for what is actually a runtime-configuration bug, and which therefore survives the operator opening the network. Any script performing network I/O must first import `scripts/lib/proxy-bootstrap.mjs` (re-execs with `NODE_USE_ENV_PROXY=1`). | The L1→L2/L3 article pipeline stopped producing on 2026-07-26 and stayed dead for 7 days / 28 consecutive dispatches: `article-level{2,3}`'s `pick-l1-source.mjs` exited 3 on every fire, while `feed-post` / `daily-research` (whose only endpoint is the AWS agents-api) kept working — the difference being api.notion.com vs. an allowlisted host. Found 2026-08-02 only because a reader asked why one specific L1 source had never been published. | 1 | promoted | R-14 (`scripts/check-proxy-bootstrap.mjs`, `ci.yml`) — every `fetch()`-calling `.mjs` must bootstrap the proxy. **§6.1 two-occurrence threshold waived deliberately**: a 7-day undetected production outage across 28 green dispatches is the severity exception, not a second sighting. Note the in-repo gate is belt-and-braces — the real fix is `NODE_USE_ENV_PROXY=1` in the agent-runner's CCR session environment, which fixes every script that will ever exist; that platform change is a follow-up outside this repo. |
| ML-018 | A queue whose consumer always takes `pending[0]` and records nothing on failure is a head-of-line stall waiting to happen: one permanently unprocessable item blocks every successor forever, and because each individual fire "succeeds" (dispatch ok, no error written) the stall is invisible. A picker over an unbounded queue must (a) step past items it cannot process, and (b) persist the failure and a per-item attempt count so a dead item eventually leaves the queue. | `pick-l1-source.mjs` returned the oldest uncovered L1 row unconditionally. When that row was reuters.com (HTTP 401 direct; the reader fallback 403), the cadence's own hard rule "if you cannot quote the source, do not publish" produced nothing — and the next fire re-picked the identical row. The L1 backlog behind it (8 rows, incl. the operator-reported lseg.com source at position 6) could never be reached. Same incident as ML-016; this was the second, independent blocker that would have kept the queue stalled even after ML-016 was fixed. | 1 | promoted | Fetch-first picker with `L2 Attempts` / `L2 Last Error` / `L2 Last Attempt` write-back and a `MAX_ATTEMPTS` block (`workforce/skills/article-level2/pick-l1-source.mjs` 0.3.0); staleness detector R-15. **§6.1 threshold waived deliberately** for the same severity reason as ML-017. |
| ML-019 | A cadence's execution outcome must be observable from outside the CCR session. The orchestrator's `dispatched[]` records that a task was *handed off*, not that it did anything: 28 dispatches produced 28 empty fires while `AGENT#ingrid.last_run_at` stayed `null` and zero RUN rows were written. Any signal derived only from dispatch success is blind to the failure modes that matter. Candidate guard beyond R-15: have the agent-runner routine write a per-fire outcome row (produced / skipped-with-reason / failed) so "N consecutive zero-deliverable fires" is queryable — the detector ML-013 also asks for. | The 2026-07-26 → 2026-08-02 article-pipeline outage was invisible to every existing signal: CI green, deploys green, orchestrator logs clean, no alert. It was found by hand. | 1 | accepted | R-15 (`scripts/check-corpus-freshness.mjs`) watches the *output* — the one observable that actually moved. The per-fire outcome ledger (shared with ML-013) remains unbuilt. |
| ML-020 | A content-integrity heuristic must cover the punctuation conventions of **every** language the corpus publishes in. The shared `scripts/lib/truncation.mjs` terminal-glyph set accepted the Japanese closing quote `」` but no English one, and English puts the full stop *inside* the quote — so every translated paragraph ending on a quotation (`…of the coming decade."`) read as a mid-sentence cut. The fix is to unwrap trailing closers before the glyph test, the same way ML-006's trailing emphasis is unwrapped, which keeps a line ending on an *opening* quote correctly flagged. Second-order lesson: this heuristic gates every deploy (R-10) and every generation (W-1) and had **zero test coverage** until now — the language gap was invisible because nothing ever asserted the accept-side behaviour. | The first production ja→en backfill run (2026-08-04) rejected 9 of 25 articles as "looks cut off" with `finish_reason: stop`; the two whose Japanese closing paragraph ends `」。` (dfd1710a003a, ed1bb8784c43) are structurally certain false positives. | 1 | watching | Fix + first-ever test suite for the heuristic (`scripts/lib/truncation.test.mjs`, pinning both the flag and the accept side). No new R-rule: the existing R-10/W-1 gates are the enforcement; this was a defect in what they measure. **Both halves of the rule now applied:** the Western closers (`" ' ” ’ »`) are unwrapped before the glyph test, and the Japanese bracket family `』】》〉` joined `」` in the *terminator* set — only `」` had ever been listed, so idiomatic complete Japanese such as `…彼は『これは構造的だ』` and `…詳細は【注】` was flagged exactly as the English quotations were. Note the closers/terminator distinction is load-bearing: unwrapping `』` off `…構造的だ』` leaves `だ`, a letter, so the closer-set fix two reviewers proposed would not have fixed it. |
| ML-021 | A guard that rejects generated content must preserve the evidence for its own rejection, and every outbound call must retry and report its real cause. Three shapes of the same defect shipped together in `backfill-en.mjs`: (a) the W-1 rejection message used the 60-char `lastNonEmptyLine` preview, eliding exactly the final characters needed to tell a true cut-off from a false positive, and discarded the translation entirely — making a *reproducible* failure undiagnosable; (b) the Azure call had no retry at all while the Notion call had four, so the long, slow requests most likely to hit a socket reset were the ones with no protection, and undici's bare `"fetch failed"` hid the real cause in `err.cause`; (c) the backoff computed `Number(headers.get('retry-after'))` without an emptiness guard — `Number(null)` is `0`, so every retry without that header fired instantly. | The first production ja→en backfill run (2026-08-04): 7 of 25 articles failed with the single unactionable word `fetch failed`, and 9 more with a truncated preview of the line that was the entire question. Both classes were reproducible per-article, and neither left anything to inspect. | 1 | watching | `scripts/lib/http-retry.mjs` (retry/backoff, cause-chain unwrapping, per-attempt timeout, `fatal` sentinel for non-retryable 4xx, and a `retryTransport`/`retryServerErrors` split so a **non-idempotent write is never retried on a transport error** — a retried `POST /pages` duplicates the `EN` child page and a retried `PATCH /blocks/{id}/children` duplicates a block chunk, both invisible to the export and to R-10) + 22 tests, adopted by both call sites in `backfill-en.mjs`; the W-1 checks extracted to `scripts/lib/en-publishable.mjs` (+11 tests) so the composition that produced ML-020's false rejections is importable at all, now reporting `finish_reason`, the quoted tail with invisibles escaped, and the full last line, and saving the rejected translation under `.backfill-en-failures/`. **Scope note — the law is stated generally and the fix is NOT yet general.** These call sites still issue bare `fetch()` with no retry and/or still print the 60-char preview: `workforce/skills/article-level{2,3}/publish-notion.mjs`, `article-level2/pick-l1-source.mjs`, `podcast-script/publish-notion.mjs` (W-1 preview), and `newsletter/pipeline/fetchers/notion.mjs` (status-only retry, no tests — and it runs inside `deploy-article-site.yml`, so a bare transport failure there aborts the whole Notion export). The cadence files are workforce-zone with their own governance, so migrating them is a follow-up, not an in-PR change. Recorded here rather than left implied, because a reader of this row would otherwise take the rule as enforced everywhere. |
| ML-022 | A router/verdict contract that is stated in more than one place must be amended in **every** place, and the place that matters most is the one consulted at the moment of the decision — not the one that reads best. A rule keyed on a term that is true of the wrong set (`non-consensus`, read as "not unanimous green", which is true of every 🔴 and every 🟡) swallows the narrower rows above it and silently voids the mechanism they encode. The same defect class as ML-016: a rule that cannot tell two situations apart, defaulting to the wrong one. Aggravating factor specific to escalation: the mis-stamp is **irreversible by construction** — `pr-autopilot-scan.mjs` `isTerminal()` keys on `autopilot:needs-human` alone, no sweep reaches an already-escalated PR, and there is no de-escalation path, so a PR mis-escalated once is invisible to the cadence forever. Candidate `R-NN` when it recurs: mandatory free text on a verdict-path `no-reviewer-consensus` (the guard `reasonMarker()` already implements for `other`), so the router must name the contradiction it claims. | (1) PR #524, 2026-07-31 07:51Z: a 🔴 whose three findings were all diff-local was stamped `no-reviewer-consensus` + `autopilot:needs-human`, ~15.5h before adr-0022 shipped the author lane — so the router genuinely had only merge-or-human, but its own verdict text said "the revision is the next step" and prescribed the cycle-2 fixes, which the label then made unproducible by the workforce. Resolved only by the operator merging it by hand on 2026-08-04. (2) The #543 panel found the *same* defect surviving one section lower in the same file: `SKILL.md` §"Reason codes", the pick-list a router reads when choosing a code, still offered `no-reviewer-consensus` unqualified and omitted the author-lane codes entirely — three isolated lenses (dario A1, mateo A1, farah A2) landed on that one paragraph independently. Occurrence 2 is the same rule failing at a second carrier before the first fix had even merged. | 2 | accepted | **This PR (#543):** the verdict-table row split so every branch names its `--reason` code; `non-consensus` → `irreconcilable panel` with a definition and the discriminating question (*is there a fix an agent could make that all lenses would accept?*); the §"Reason codes" pick-list qualified and given the four author-lane codes; the unanimous premise/scope 🔴 given an explicit home (`other` + free text); `pr-escalation-reasons.md` rescoped and dated `v3.1` so the Epic-019 series has a visible boundary. **Owed next:** the mechanical half — free text required on a verdict-path `no-reviewer-consensus`, and a de-escalation path so ML-009's guard can tell "never labelled" from "resolved and returned". Both filed as **FU-036**; the de-escalation half needs operator approval because it loosens an existing guard. |
| ML-023 | A mutation harness must run in a throwaway checkout, never in the tree you commit from. Applying a mutation, running the suite, and restoring is a *write* to the working tree, so it races anything else reading or committing that tree — and the restore step is exactly the part that can fail silently. Two distinct failure modes were observed from one root cause: (a) restoring with `git checkout -- <file>` reverts to **HEAD**, not to the pre-mutation state, so on a branch whose HEAD predates the fixes under test it silently discards them and every subsequent probe reports against unfixed source — the "survived/caught" verdicts are then meaningless while looking authoritative; (b) a concurrent reader (a review subagent, a dev server, a watch-mode runner) sees a mutated tree and reasons from it, or a commit lands mid-window and ships the mutation. Candidate guard when it recurs: run mutation probes in `git worktree add` / a temp clone, and assert `git status --porcelain` is empty on harness exit. | PR #554, 2026-08-06, pr-autopilot cycle 1→2 on `/org/chart`: (a) the author's first probe loop used `git checkout -- <file>` between mutations, reverting the two source files to the merged-`main` state — probes 2–5 ran against unfixed source and their results were void; caught by the author, fixes re-applied, all probes re-run from `cp` backups. (b) During the cycle-2 panel, `wf:dario` read `OrgChart.tsx` with the O6 termination guard absent and then found an **unreverted** O2 mutation (the slug clause `agent.slug.toLowerCase().includes(q)` replaced by a constant `false`, leaving the rest of the disjunction intact) persisting in `orgTree.ts` across 24s of polling, while `wf:owen` independently found the D1 mutant reappearing after it had verified the tree clean and had to re-run every "survived" verdict inside a single shell invocation with pre/post md5 checks. Both lenses re-read every file via `git show HEAD:` and both reported the final tree clean; no mutation reached a commit. | 1 | watching | — Two symptoms, one harness run, one incident window — counted as **one** occurrence, not two: (a) and (b) share a single root cause (the harness writing to the tree it commits from), so counting them separately would promote a gate on one data point. A second inside 90 days is the §6.1 ratchet's cue. The immediate discipline is stated in the rule: worktree-isolate the harness and assert a clean tree on exit.) |
| ML-025 | A mechanical gate whose population is defined by a *syntactic* marker misses every member of the class that reaches the same behaviour *transitively*. R-14 (`scripts/check-proxy-bootstrap.mjs`) defines its population as "files containing a real global `fetch(` call", so an **entry script whose network I/O is entirely delegated to an imported helper** is not in the population at all — the gate reports green while the script bypasses the proxy and 401s on every fire. `ensureProxyAwareEntry` compounds it by design: it correctly no-ops when the importing module is not the process entry, so the helper's own bootstrap call never fires on the delegating entry's behalf. A gate over "does this file call X" is a gate over *syntax*, never over *behaviour*; when the two diverge, widen the population to the entry scripts that transitively reach X. Candidate `R-NN` on a third occurrence: extend R-14's population to any `.mjs` with a CLI entry guard that imports a bootstrapped module. | (1) ML-017, 2026-08-02 — R-14 was created precisely because a green check had never measured the failing case. (2) 2026-08-03 (#534, surfaced by `backlog-reconcile` from a sibling `pr-autopilot` fire in the same batch): `pr-autopilot-sweep.mjs` had zero `fetch(` occurrences — it fetches via `makeGh()` imported from `pr-merge.mjs` — so R-14 excluded it, `NODE_USE_ENV_PROXY=1` was never set, and Step 6 of the pr-autopilot contract exited 3 on `GET pulls → HTTP 401` on **every** fire. The token was never bad; which script the run reached decided whether the fire threw. #548 applied #534's *point* fix (bootstrap import in the sweep + `budget-runway-review/post.mjs`) and explicitly deferred the *gate* fix, which was #534's own preferred option. | 2 | accepted | **Not yet promoted — the gate half is still open.** Verified 2026-08-06 on `main` (`231d8e3`): `check-proxy-bootstrap.mjs` reports `✅ R-14: all 75 fetch()-calling script(s) bootstrap the proxy correctly` while four entry scripts in the identical class remain unbootstrapped and will 401 the same way — `pr-remediate/pr-remediate-scan.mjs`, `pr-remediate/pr-remediate-post.mjs`, `issue-triage/issue-triage-scan.mjs`, `issue-triage/issue-triage-post.mjs` (each: 0 direct `fetch(`, 0 bootstrap import; `issue-triage-scan.mjs:22` imports `makeGh` from `pr-merge.mjs`, the identical shape as the sweep). Tracked by #534, rescoped this pass to the remaining gate half. |
| ML-026 | A CI freshness gate that runs **after** a step which regenerates the artefact it checks is structurally incapable of failing. The check must read the committed state (`git show HEAD:<path>`), or be ordered before any step that writes the artefact. | 2026-08-04 backlog-reconcile: PR #528 added `Workforce skill manifest freshness (#185)` at `.github/workflows/ci.yml:70`, below `Typecheck + build` at `:29` whose `prebuild` chain rewrites `workforce/app/public/workforce-skills.json`. The gate reported OK on PR #536 (job 91823811252) 30s after regenerating its own input, while `main` carried a manifest stale for 29 of 39 skills. Issue #185 was closed against an acceptance criterion the gate could not verify. | 1 | accepted | — (occurrence 1; promote to an `R-NN` statute row on a second occurrence within 90 days, per §6.1) |
| ML-027 | A registry id (`ML-NN`, `FU-NNN`, `ADR-NNNN`) picked at **branch** time is validated at **merge** time, so parallel PRs each take "highest free + 1" against a base that has since moved and collide silently. Nothing fails: each branch is internally well-formed, `check-governance-registries.mjs` passes on both sides, and the collision surfaces only as a merge conflict — or, if the rows land in different table regions, not at all. Either an id is allocated against the *base at merge*, or the allocation is serialised. | (1) `ML-023` claimed simultaneously by #556 (merged, kept it), #547 (renumbered → `ML-024`) and #550 (renumbered → `ML-025`), 2026-08-06. (2) `ML-020` + `FU-036` + `FU-037` all claimed by #537 against ids `main` had taken via #543 — three collisions in one PR, renumbered → `ML-026` / `FU-037` / `FU-038`, same day. | 2 | accepted | **Process, applied now:** registry-carrying PRs are handled one at a time — review → fix → merge → next — rather than reviewed in parallel, so each allocation sees the previous one's merge. **Mechanical candidate, not yet built:** extend `check-governance-registries.mjs` with a `--vs-base` mode that diffs the PR head's new ids against the base branch's and fails on an id present in both. A per-branch check cannot catch this class; it needs the base. |
| ML-028 | A batched `agent-runner` fire (workforce/docs/routines/agent-runner.md) that dispatches one subagent per task in parallel for isolation must give each subagent its own working/scratch path. A shared scratchpad directory lets a faster sibling's temp write (e.g. a generic `body.md` / `engagement.json` filename) collide with and overwrite another task's in-flight file; if the engagement POST body **or a skill's product write** (feed post, article, verdict — not just the engagement row) is then built by re-reading that shared file rather than kept in the task's own memory, the wrong task's content lands under the *victim* task's `agent_slug`, either as a ledger misattribution or as **live, publicly-visible content published under the wrong identity** — and neither the `POST /agents/{slug}/engagements` write surface nor `POST /feed` has a delete/correction endpoint. | 2026-08-13T23:29:57Z batched fire (9 tasks): Jay's Bihar/BERC-order content POSTed under `marisol`'s `agent_slug` (engagement row `J5K6PC9WRMMDQME11C148894QH`); caught via self-read-back, no delete available, superseded by a correct second engagement (`B25YTH34BM8FSH4GRASSBQJBP8`) built by inlining JSON instead of re-reading the shared path. Bad row still live. — 2026-08-20T01:29:57Z batched fire (19 tasks, one subagent per task): the *same* class hit the **feed-post product write itself**, not just the engagement ledger, four independent times: (1) `sneha` — first `post-feed.mjs` run published another task's unrelated draft under `agent_slug=sneha`, post `00MT0UNDXZ0000010000000000` still live; (2) `celeste` — first run published a different agent's CBAM/carbon-market content under `agent_slug=celeste`, post `00MT0UO6YC0000000000000000` still live; (3) `grace` (daily-research binding) — first run published content authored by persona **`ishaan`**, who was not a task in this fire's payload at all, under `agent_slug=grace`, post `00MT0UQXIA0000090000000000` still live — direct evidence the collision source can be a genuinely separate, concurrently-firing CCR session, not only a sibling subagent inside the same runner; (4) `marisol` — a stray diagnostic `node -e` POST issued while troubleshooting an unrelated transient `fetch failed` reused the real `FEED_WRITE_TOKEN` and created a live junk post (`00MT0UOMDN0000020000000000`, body `"test"`) under her own identity — self-caused, same root enabler (no isolated scratch path forced a bespoke diagnostic instead of a safe retry). All four callers caught the mismatch only via a post-write content read-back and superseded it; none could delete the stray artefact. All four stray posts remain live pending operator/infra cleanup. | 5 | accepted | Protocol fix landed in `workforce/docs/routines/agent-runner.md` step 5 (this PR): every task must derive a task-unique temp-file path (`agent_slug` + `binding_idx` + `ticked_at` + a random suffix, or `mktemp`) instead of a skill body's illustrative literal path, and must content-diff the deliverable back from its own store (not just the step-8 engagement read-back) before trusting a write script's exit code. **Not a numbered `R-NN` CI gate** — the failure is a runtime session/process behavior (temp-file naming inside an agent's own tool calls, and in incident (3) above, isolation *between separate CCR sessions* sharing a host) with no git-diff surface for a mechanical lint to inspect; a repo-side check can't see it. Flagged for the operator: a stronger fix (giving each task its own OS-level tmpdir at the orchestration layer, or moving body-passing off the filesystem entirely — e.g. stdin — would remove the race by construction rather than by every task remembering a naming convention) is worth scoping if this recurs after the protocol fix ships. |
| ML-029 | The agents-api list endpoints (`GET /agents/{slug}/posts` and `GET /agents/{slug}/executions`) return a **stale row — not the most recent** — when queried with a minimal page size immediately after a write from the *same request flow* (`?page_size=1` on `/posts`, `?limit=1` on `/executions`). The caller's own just-written row is missing from the result; a larger `page_size`/`limit` (3–10) on the identical query, seconds later, correctly places the new row first. Self-corrects on retry, so it never silently confirmed a wrong write in the incidents below — but a step-8/content read-back that trusts a `limit=1`/`page_size=1` result at face value (the shape the agent-runner.md guidance's own examples show) *would* wrongly read this as an ML-028-class mismatch, or worse, miss a genuine one underneath the noise. Candidate guard: a regression test against `workforce/lambdas/agents-api/handler.ts`'s posts/executions list path asserting `page_size=1`/`limit=1` returns the same head row as a larger page size, immediately after a write in the same test — now tracked as [#613](https://github.com/refluster/ai-native-article/issues/613). | 2026-08-20T13:29:57Z batched `agent-runner` fire (8 tasks) — five independent hits across four different personas' sessions, all self-diagnosed and worked around by re-querying with a larger page/limit rather than trusted at face value: (1) `farah` — `GET /agents/farah/posts?page_size=1` returned an unrelated 2026-05-26 post instead of the just-written one; confirmed correct via `page_size=10` and `/feed`. (2) `aoi` — `GET /agents/aoi/posts?page_size=1` triggered a "stale-cache false alarm" on first read; resolved by cache-busting/re-query, top post then matched byte-for-byte. (3) `vikram` — `GET /agents/vikram/posts?page_size=1` "oddly returned a stale/oldest row"; resolved via `page_size=3`. (4) `levi` — same symptom on `posts?page_size=1` (returned an old May-2026 post); resolved via `page_size=3`. (5) `grace` (second of two bindings in this fire) — `GET /agents/grace/executions?limit=1` returned the *sibling* binding's engagement row instead of her own just-written one; resolved via `limit=5`, own row present as the second-most-recent entry with all fields matching. **Second incident window:** 2026-08-21T23:29:57Z batched `agent-runner` fire (9 tasks) — three further independent hits, all self-corrected the same way: (6) `sora` — `posts?page_size=1` returned a stale/oldest post; resolved via `page_size=3`. (7) `yuki` — same symptom; a `page_size=3` re-read ~2s later returned the correct newest post. (8) `kai` — reproduced **twice** in one session: returned a fixture-looking seed post from 2026-05-25 (`01J0P0FEED14KAI001`); resolved via `page_size=3`. | 2 | promoted | [#613](https://github.com/refluster/ai-native-article/issues/613). Root cause: neither base-table `QueryCommand` in `workforce/lambdas/shared/ddb.ts` requested `ConsistentRead`, so a read racing a same-flow write could be served from a lagging replica. **Fixed** for `GET /agents/{slug}/posts` (`queryBySkPrefix` / `queryBySkPrefixPaged`, the primitive behind 7 of the 8 logged hits) — both now set `ConsistentRead: true`, locked in by `workforce/lambdas/shared/ddb-tests.ts` (asserts the flag on every `QueryCommand` those two helpers send; runs under the existing `npm test` / ci.yml "Workforce Lambda unit tests" step — no new `R-NN` needed, same shape as ML-020's "defect in what the existing gate measures"). **NOT fixed, and structurally can't be** for `GET /agents/{slug}/executions` (`queryByGsi`/GSI1) — DynamoDB rejects `ConsistentRead: true` on any GSI query outright, so that endpoint's stale-head-row risk is a permanent AWS platform limitation, now documented on `queryByGsi` in ddb.ts and asserted (`ConsistentRead` absent) by the same test file. Callers reading their own just-written EXEC row immediately should keep over-fetching (`limit` ≥ 3–5) rather than trust `limit=1`, exactly as `agent-runner.md`'s step-8 read-back guidance now does (fixed by [#616](https://github.com/refluster/ai-native-article/issues/616): `limit=5`, asserting the just-sent row is present in the page rather than the head row). |
| ML-030 | A one-shot write endpoint (`POST /agents/{slug}/engagements`, and by the same shape `POST /feed`) is **not idempotent**, so re-issuing the identical `curl` call a second time — for any reason, including just to capture the HTTP status code the first invocation already returned and discarded — creates a second live row with the same content under the same `agent_slug`. The agent-runner.md step-8/12 read-back verifies the *content* of the top row matches what was sent and can pass fully clean while missing that the write is duplicated, because it only diffs one row against the intended body — it never checks how many rows a single logical write produced. | 2026-08-21T15:29:57Z batched `agent-runner` fire (10 tasks): `grace`'s `daily-research` task recorded its engagement once via `POST /agents/grace/engagements` (`201`), then issued a second, separate `curl` call against the identical endpoint/body solely to inspect the HTTP status code — not realizing the re-run itself re-submitted the write — producing two `EXEC#*` rows (`ZYZQPX6N7MVN4RMQJE4DQRJHNY`, `KSB7AE6J08MT37VVGNHHMM1AP2`) that both point at the single feed post `00MT34736X0000040000000000`. Content on both rows is correct and attributed to the right agent (not an ML-028-class misattribution) — only the row count is doubled. No delete-engagement capability was available to the task to clean it up; the duplicate row is flagged here for operator cleanup. | 1 | watching | — (occurrence 1; the fix is procedural, not mechanical — capture the status from the single write call itself, e.g. `curl -s -o body.json -w '%{http_code}' ...` on the *original* POST, rather than ever re-issuing a POST to inspect its result. Worth adding as an explicit caution on agent-runner.md step 8 on a second occurrence within 90 days; a repo-side check can't see a runtime curl invocation choice, so this stays a memory item, not an `R-NN` gate, unless the pattern recurs.) |
| ML-031 | A change to **where the site is served from** (`SITE_BASE_PATH`) is only half-checkable inside the repo: the literals can be internally consistent and still point at a path GitHub Pages does not publish this build under, because the custom domain is held by whichever repo GitHub Settings says holds it — a fact no diff contains. Both incidents produced the same symptom (a reader-visible blank page behind a green deploy) from opposite directions: a base that was right for a URL nobody was serving, then a base that was wrong for the URL everybody uses. | #600 — the apex `kohuehara.xyz` served the user site's 2025 CRA build while gh-pages held the current one (2026-08-16); PR #606 — SITE_BASE_PATH and all five static copies moved to `/` on the premise that `cname:` in the deploy workflow claims the apex; it does not, so `https://kohuehara.xyz/ai-native-article/` served an index.html whose every asset 404'd for a week until a reader reported it (2026-08-17 → 2026-08-24) | 2 | promoted | R-16 (`scripts/check-base-path.mjs`, literals agree with each other — `ci.yml`) + R-17 (`scripts/check-live-base-path.mjs`, the deployed shell and its assets actually load — daily `corpus-freshness.yml`). Neither alone is sufficient: see governance.md §4 Policy. |
| ML-032 | A post-deploy check that asserts on *served* content must know **which build** it is looking at. GitHub Pages answers 200 with the **previous** build for minutes after `actions-gh-pages` reports success, and that stale build is normally healthy — so both naive readings are wrong: failing on it is a false red on every deploy, and retrying merely until nothing fails is a false green that signs off a deploy the gate never looked at. The check must settle on build **identity** (does the served shell reference the hashed assets this run published?) and refuse to conclude when it never sees them. | R-17's first real run: the #619 merge deploy (2026-08-24 14:07 UTC) went red listing the *pre-merge* asset hashes, on a site that served correctly ~4 min later; then #620's first fix (retry-while-failing) was reproduced going **green** against a healthy stale build while the new build was broken — caught in review, never shipped | 1 | watching | Mitigated in-place by `R17_EXPECT_BUILD_DIR` + `R17_SETTLE_ATTEMPTS` (identity settle, 10 x 30s post-deploy; 1 = off for the daily run, where a failure is never propagation), with the loop extracted to the unit-tested `scripts/lib/settle.mjs` and the race reproducible via `scripts/lib/pages-propagation-fixture.mjs`. Latency cost accepted as RAL-005. Promote to an `R-NN` — a shared settle-on-identity helper every post-deploy assertion uses — if a second post-deploy check hits the same race. |
