name: Workforce SAM Deploy

on:
  pull_request:
    paths:
      - 'workforce/infra/sam/**'
      - 'workforce/lambdas/**'
      - '.github/workflows/workforce-deploy.yml'
  push:
    branches: [main]
    paths:
      - 'workforce/infra/sam/**'
      - 'workforce/lambdas/**'
      - '.github/workflows/workforce-deploy.yml'
  workflow_dispatch:
    inputs:
      stage:
        description: 'Deployment stage'
        required: true
        default: dev
        type: choice
        options: [dev, prod]

permissions:
  id-token: write
  contents: read

concurrency:
  group: workforce-deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    name: SAM validate + typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: workforce/lambdas/package-lock.json

      - name: Install lambda deps
        working-directory: workforce/lambdas
        run: npm ci

      - name: Install esbuild globally (SAM esbuild builder needs it on PATH)
        run: npm install -g esbuild@0.24.2

      - name: TypeScript typecheck
        working-directory: workforce/lambdas
        run: npm run typecheck

      - uses: aws-actions/setup-sam@v2
        with:
          use-installer: true

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: SAM validate
        working-directory: workforce/infra/sam
        run: sam validate --lint

      - name: SAM build
        working-directory: workforce/infra/sam
        run: sam build --parallel

      - name: SAM dry-run (PR only)
        if: github.event_name == 'pull_request'
        working-directory: workforce/infra/sam
        env:
          REGION: ${{ vars.AWS_REGION }}
        run: |
          # Skip dry-run if the stack is in a state CFN cannot update.
          # The operator must `aws cloudformation delete-stack` and the
          # next push-to-main / workflow_dispatch will recreate it.
          STATUS=$(aws cloudformation describe-stacks \
            --stack-name workforce-dev \
            --region "$REGION" \
            --query 'Stacks[0].StackStatus' \
            --output text 2>/dev/null || echo "NOT_FOUND")
          case "$STATUS" in
            ROLLBACK_COMPLETE|UPDATE_ROLLBACK_FAILED|ROLLBACK_FAILED|CREATE_FAILED|DELETE_FAILED)
              echo "::warning::Stack workforce-dev is in $STATUS — dry-run skipped. Operator must run 'aws cloudformation delete-stack --stack-name workforce-dev --region $REGION' before the next deploy can succeed."
              exit 0
              ;;
          esac
          sam deploy \
            --no-execute-changeset \
            --no-confirm-changeset \
            --capabilities CAPABILITY_IAM \
            --parameter-overrides Stage=dev \
            --stack-name workforce-dev \
            --resolve-s3

  deploy-dev:
    name: Deploy dev stack
    runs-on: ubuntu-latest
    needs: validate
    # Runs on push to main (not PRs) and on manual dispatch for dev
    if: |
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.stage == 'dev')
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: workforce/lambdas/package-lock.json

      - name: Install lambda deps
        working-directory: workforce/lambdas
        run: npm ci

      - name: Install esbuild globally (SAM esbuild builder needs it on PATH)
        run: npm install -g esbuild@0.24.2

      - uses: aws-actions/setup-sam@v2
        with:
          use-installer: true

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: SAM build
        working-directory: workforce/infra/sam
        run: sam build --parallel

      - name: SAM deploy (dev)
        working-directory: workforce/infra/sam
        run: |
          sam deploy \
            --no-confirm-changeset \
            --no-fail-on-empty-changeset \
            --capabilities CAPABILITY_IAM \
            --parameter-overrides Stage=dev \
            --stack-name workforce-dev \
            --resolve-s3

  deploy-prod:
    name: Deploy prod stack
    runs-on: ubuntu-latest
    needs: validate
    environment: prod
    # Only on manual dispatch requesting prod
    if: |
      github.event_name == 'workflow_dispatch' && github.event.inputs.stage == 'prod'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: workforce/lambdas/package-lock.json

      - name: Install lambda deps
        working-directory: workforce/lambdas
        run: npm ci

      - name: Install esbuild globally (SAM esbuild builder needs it on PATH)
        run: npm install -g esbuild@0.24.2

      - uses: aws-actions/setup-sam@v2
        with:
          use-installer: true

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: SAM build
        working-directory: workforce/infra/sam
        run: sam build --parallel

      - name: SAM deploy (prod)
        working-directory: workforce/infra/sam
        run: |
          sam deploy \
            --no-confirm-changeset \
            --no-fail-on-empty-changeset \
            --capabilities CAPABILITY_IAM \
            --parameter-overrides Stage=prod \
            --stack-name workforce-prod \
            --resolve-s3
